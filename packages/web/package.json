{
  "name": "web",
  "version": "1.0.0",
  "scripts": {
    "build": "rimraf build && sh scripts/build.sh",
    "start": "NODE_PATH=build serve build"
  },
  "devDependencies": {
    "minify": "^10.0.0",
    "rimraf": "^5.0.1",
    "serve": "^14.2.0",
    "tailwindcss": "^3.3.2"
  },
  "engines": {
    "node": "16.x"
  },
  "packageManager": "yarn@3.2.1",
  "license": "MIT"
}
